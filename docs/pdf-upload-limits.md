# PDF 업로드 한도

- 직접 올리는 PDF: 최대 200MB (200 × 1024 × 1024바이트).
- 브라우저, Blob 업로드 토큰, 로컬 PDF 저장 API는 `lib/lecture/uploadLimits.ts`의 공통 제한을 사용한다.
- 파일을 메모리에 읽기 전에 크기를 검사한다. 배포 환경에서는 기존 multipart 방식으로 Blob에 직접 전송한다.
- 수업은 삽입한 페이지를 포함해 최대 1,000장이다.
- PPTX 변환 결과의 60MB 제한은 별도다. 이번 변경은 직접 PDF 업로드 한도를 높인다.
